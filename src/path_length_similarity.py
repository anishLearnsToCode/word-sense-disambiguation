# Method 3 (Path Length Similarity)
# The simple LESK Algorithm Computes the similarity between 2 given synsets by calculating the hop distance
# between the 2 synsets in the graphs and then the similarity is computed as -log()
